# AI1Net Workspace

## Overview

pnpm workspace monorepo using TypeScript. **AI1Net** is a neo-brutalist AI hub dashboard ("Bloomberg Terminal of AI") with multi-provider AI tools, token economy ($AI1NET), usage tracking, rewards, staking, and governance.

## Stack

- **Monorepo tool**: pnpm workspaces
- **Node.js version**: 24
- **Package manager**: pnpm
- **TypeScript version**: 5.9
- **Frontend**: React + Vite (Tailwind v4 neo-brutalism, `@tailwindcss/vite`)
- **Auth**: Clerk (Replit-managed, `@clerk/react` + `@clerk/express`)
- **API framework**: Express 5
- **Database**: PostgreSQL + Drizzle ORM
- **Validation**: Zod (`zod/v4`), `drizzle-zod`
- **API codegen**: Orval (from OpenAPI spec → React Query hooks)
- **Build**: esbuild (ESM bundle)

## Artifacts

| Artifact | Path | Port | Description |
|---|---|---|---|
| `ai1net` (web) | `/` | 23717 | React+Vite frontend |
| `api-server` (api) | `/api` | 8080 | Express 5 backend |

## Key Commands

- `pnpm run typecheck` — full typecheck across all packages
- `pnpm run build` — typecheck + build all packages
- `pnpm --filter @workspace/api-spec run codegen` — regenerate API hooks and Zod schemas from OpenAPI spec
- `pnpm --filter @workspace/db run push` — push DB schema changes (dev only)
- `pnpm --filter @workspace/api-server run dev` — run API server locally

## Database Schema

Tables: `users`, `ai_providers`, `ai_tools`, `ai_usage`, `tokens`, `rewards`, `stakes`, `governance_proposals`, `governance_votes`, `activity_logs`

Seeded with: 5 AI providers, 12 AI tools, 4 governance proposals.

## Key Architecture Notes

- **Clerk VITE_CLERK_PUBLISHABLE_KEY**: Defined via `vite.config.ts` `define` block reading `process.env.CLERK_PUBLISHABLE_KEY`. This is required so Clerk uses the explicit dev key instead of deriving it from hostname (which would wrongly enable the proxy subdomain in dev).
- **Tailwind v4**: Uses `@layer theme, base, clerk, components, utilities;` at top of `index.css`, `tailwindcss({ optimize: false })` in vite config for Clerk theme compatibility.
- **Custom CSS utilities**: `brutalist-border`, `brutalist-shadow`, `brutalist-card`, `brutalist-button` defined in `@layer utilities` in `index.css`.
- **Clerk proxy**: Only active in production; `clerkProxyMiddleware` mounted at `/api/__clerk`.
- **API client**: All hooks generated by Orval in `lib/api-client-react/src/generated/`. `lib/api-zod/src/index.ts` exports only from `./generated/api` (types export removed to avoid name collision).
- **Auth flow**: `requireAuth` middleware checks Clerk JWT. `getOrCreateUser` helper syncs Clerk user to DB.

See the `pnpm-workspace` skill for workspace structure and TypeScript setup.
